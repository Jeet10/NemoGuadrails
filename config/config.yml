models:
  - type: main
    engine: nim
    model: deepseek-ai/deepseek-r1      # Align this with the llm you instantiate in chat.py
    parameters:
      api_key: "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"
      nim_base_url: "https://ai.api.nvidia.com"

  # Optional: if you still want a separate guard model, keep it. Otherwise remove.
  - type: llama_guard
    engine: nim
    model: deepseek-ai/deepseek-r1
    parameters:
      api_key: "${NVIDIA_API_KEY}"
      nim_base_url: "https://ai.api.nvidia.com"

rails:
  input:
    flows: []
  output:
    flows:
      - self check facts   # Enable the fact-check output rail flow (library-provided)

lowest_temperature: 0.1

# If you add more task-specific customizations, keep them under prompts.yml.